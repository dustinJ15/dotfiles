# Change prefix to Ctrl-a (much easier to reach)
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Enable mouse support
set -g mouse on

# Clean status bar
set -g status-left "[#S] "
set -g status-left-length 20

# Easy splitting
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Increase history
set -g history-limit 10000

# Design tweak: subtle gray status bar
set -g status-style bg='#333333',fg='#aaaaaa'
