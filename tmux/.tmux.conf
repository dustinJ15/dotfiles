# Change prefix to Ctrl-a (much easier to reach)
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Enable mouse support
set -g mouse on
bind -T copy-mode MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "wl-copy"
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "wl-copy"

# Clean status bar
set -g status-left "[#S] "
set -g status-left-length 20

# Easy splitting
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Vim-style pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Increase history
set -g history-limit 10000

# Design tweak: subtle gray status bar
set -g status-style bg='#333333',fg='#aaaaaa'

# Allow passthrough for Kitty graphics protocol (image preview in Neovim)
set -g allow-passthrough all
